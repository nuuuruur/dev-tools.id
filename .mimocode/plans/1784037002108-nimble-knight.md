# Plan: Fix Checkbox Rendering for GitHub

## Problem
`[x]` in markdown tables renders as literal text on GitHub, not a checkbox.

## Solution
Replace with Unicode symbols in `generate.py` line 72:

```python
# Before
checkbox = "x" if item.get("tried") else " "

# After
checkbox = "✅" if item.get("tried") else "⬜"
```

## File to Modify
- `generate.py` — line 72

## Verification
Run `gen` and check README.md renders ✅ / ⬜ in the Status column.
